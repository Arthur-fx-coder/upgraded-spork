{
  "name": "workers",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker application",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "vitest": "^1.3.1",
    "wrangler": "^3.28.0"
  }
}
